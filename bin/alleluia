#!/usr/bin/env node

var run = require('../lib');

global.env = {
  config: "alleluia.json"
};

var config = require('../lib/config');

try {
  run(config);
} catch(e) {
  console.error(e);
  process.exit(1);
}